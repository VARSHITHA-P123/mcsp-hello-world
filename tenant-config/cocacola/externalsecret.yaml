apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cocacola-secrets
  namespace: cocacola
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-secret-store
    kind: ClusterSecretStore
  target:
    name: cocacola-secrets
    creationPolicy: Owner
  data:
    - secretKey: customerName
      remoteRef:
        key: cocacola-source-secret
        property: customerName
    - secretKey: apiKey
      remoteRef:
        key: cocacola-source-secret
        property: apiKey
    - secretKey: dbPassword
      remoteRef:
        key: cocacola-source-secret
        property: dbPassword