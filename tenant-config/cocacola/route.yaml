apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcsp-app
  namespace: cocacola
  labels:
    app: mcsp-app
    tenant: cocacola
spec:
  to:
    kind: Service
    name: mcsp-app
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect