apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: customer-c-secrets
  namespace: customer-c
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-secret-store
    kind: ClusterSecretStore
  target:
    name: customer-c-secrets
    creationPolicy: Owner
  data:
    - secretKey: customerName
      remoteRef:
        key: customer-c-source-secret
        property: customerName
    - secretKey: apiKey
      remoteRef:
        key: customer-c-source-secret
        property: apiKey
    - secretKey: dbPassword
      remoteRef:
        key: customer-c-source-secret
        property: dbPassword