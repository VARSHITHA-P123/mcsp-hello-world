apiVersion: v1
kind: Service
metadata:
  name: mcsp-app
  namespace: customer-a
  labels:
    app: mcsp-app
    tenant: customer-a
spec:
  selector:
    app: mcsp-app
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP