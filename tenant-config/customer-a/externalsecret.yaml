apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: customer-a-secrets
  namespace: customer-a
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-secret-store
    kind: ClusterSecretStore
  target:
    name: customer-a-secrets
    creationPolicy: Owner
  data:
    - secretKey: customerName
      remoteRef:
        key: customer-a-source-secret
        property: customerName
    - secretKey: apiKey
      remoteRef:
        key: customer-a-source-secret
        property: apiKey
    - secretKey: dbPassword
      remoteRef:
        key: customer-a-source-secret
        property: dbPassword