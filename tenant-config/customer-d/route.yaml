apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcsp-app
  namespace: customer-d
  labels:
    app: mcsp-app
    tenant: customer-d
spec:
  to:
    kind: Service
    name: mcsp-app
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect