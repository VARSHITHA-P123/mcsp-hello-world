apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pepsi-secrets
  namespace: pepsi
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: kubernetes-secret-store
    kind: ClusterSecretStore
  target:
    name: pepsi-secrets
    creationPolicy: Owner
  data:
    - secretKey: customerName
      remoteRef:
        key: pepsi-source-secret
        property: customerName
    - secretKey: apiKey
      remoteRef:
        key: pepsi-source-secret
        property: apiKey
    - secretKey: dbPassword
      remoteRef:
        key: pepsi-source-secret
        property: dbPassword