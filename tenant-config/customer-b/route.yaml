apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: mcsp-app
  namespace: customer-b
  labels:
    app: mcsp-app
    tenant: customer-b
spec:
  to:
    kind: Service
    name: mcsp-app
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect